# DiceFlow — Architecture Specification
## 1. Назначение
DiceFlow — ассистент для варгеймов, предназначенный для быстрых, удобных и приятных бросков кубиков.
Приложение уже имеет проверенную предыдущую версию. Новая версия развивает существующий продукт, сохраняя его основной workflow и улучшая UX, Game Feel, производительность и расширяемость.
Главные требования:
- мгновенный запуск;
- работа через браузер;
- установка на устройство как PWA;
- работа офлайн;
- минимальное энергопотребление;
- плавная 3D-графика и анимации;
- простая разработка и сопровождение с помощью AI;
- возможность расширения без переписывания ядра.
---
## 2. Архитектурные принципы
### Разделение логики и представления
Игровая логика не должна зависеть от React, Three.js, DOM, WebGL или конкретного способа отображения.
```text
DiceFlow Core
      │
      ├── Game State
      ├── Dice
      ├── Groups
      ├── Selection
      ├── Actions
      ├── History
      └── Undo / Redo

UI и rendering являются отдельными слоями.

                 DiceFlow Core
                       │
          ┌────────────┼────────────┐
          ↓            ↓            ↓
         UI         Renderer      Storage
       React       Three.js      IndexedDB

Данные важнее визуального состояния

Источник истины — состояние игры.

Визуальные объекты не являются источником игровых данных.

User Action
    ↓
Action
    ↓
Game State
    ↓
UI / Renderer

AI-friendly architecture

Код должен быть:

* модульным;
* типизированным;
* предсказуемым;
* с небольшими файлами и функциями;
* без скрытого состояния;
* с минимальным количеством магии;
* с чёткими интерфейсами между слоями.

Каждый модуль должен иметь одну понятную ответственность.

⸻

3. Технологическая основа

Предпочтительный стек:

Language       TypeScript
UI             React
Build          Vite
3D             Three.js
Rendering      WebGL
PWA            Service Worker + Web App Manifest
State          Pure TypeScript / reducer-based architecture
Local Storage  IndexedDB
Testing        Vitest
Repository     Git

Технологии должны оставаться заменяемыми.

Игровое ядро не должно зависеть от React или Three.js.

⸻

4. Общая архитектура

src/
│
├── config.ts
│
├── core/
│   ├── dice/
│   ├── groups/
│   ├── selection/
│   ├── actions/
│   ├── history/
│   └── game/
│
├── ui/
│   ├── components/
│   ├── panels/
│   └── screens/
│
├── renderer/
│   ├── scene/
│   ├── dice/
│   ├── animation/
│   ├── effects/
│   └── postprocessing/
│
├── input/
│
├── storage/
│
├── services/
│
├── pwa/
│
└── app/

⸻

5. Core

Core — полностью независимый от платформы игровой движок DiceFlow.

Он не должен импортировать:

* React;
* Three.js;
* DOM API;
* WebGL;
* browser-specific APIs.

Core должен быть обычным TypeScript.

⸻

5.1 Dice

MVP использует только D6.

Однако модель кубика не должна быть жёстко привязана к шести значениям.

Концептуально:

Dice
├── id
├── type
├── value
├── selected
└── origin / operation metadata

В MVP:

DiceType = D6
Values = 1..6

Архитектура должна позволять в будущем иметь:

* D4;
* D6;
* D8;
* D10;
* D12;
* D20;
* специальные варгеймовые кубики;
* произвольные значения;
* дополнительные свойства граней.

Эта функциональность сейчас не реализуется.

⸻

6. Game State

Game State является единственным источником истины.

Он должен содержать только данные, необходимые для восстановления текущего состояния игры.

Концептуально:

GameState
├── dice
├── groups
├── selection
├── currentOperation
└── history

Render state не должен смешиваться с Game State.

Например:

"кубик сейчас вращается"

является состоянием Renderer, а не игровым состоянием.

⸻

7. Groups

Для D6 игровое поле состоит из шести групп:

6
5
4
3
2
1

Кубик с определённым значением автоматически отображается в соответствующей группе.

Группа является логическим понятием, а не обязательно отдельным объектом UI.

⸻

8. Selection

Система выбора должна поддерживать:

* отдельный кубик;
* несколько кубиков;
* диапазон кубиков;
* всю группу;
* несколько групп;
* диапазон групп.

Selection является частью Game State.

Visual highlighting является ответственностью Renderer/UI.

Тап по уже выбранному кубику снимает выделение. Свайп с кубика на кубик выбирает диапазон между ними.

⸻

9. Actions

Все изменения состояния выполняются через действия.

Основные действия MVP:

Roll
ReRoll
Add
Delete
Move
Clear
Select
Undo
Redo

Каждое изменение должно быть представлено как понятная операция.

Это позволит:

* реализовать Undo/Redo;
* формировать историю;
* тестировать операции отдельно;
* воспроизводить состояния;
* упрощать работу AI с кодом.

⸻

10. Roll

Roll начинает новую итерацию броска.

Roll бросает выбранные кубики и удаляет все невыбранные — они больше не участвуют.

При отсутствии выбора кнопка Roll неактивна.

После получения значения кубик попадает в соответствующую группу.

Физическая симуляция кубиков не требуется.

Кубик визуально может вращаться, но результат определяется игровым ядром.

Roll
 ↓
Random values
 ↓
Game State update
 ↓
Renderer animation
 ↓
Final visual state

⸻

11. ReRoll

ReRoll перебрасывает выбранные кубики.

Если не выбран ни один кубик, ReRoll перебрасывает все кубики на столе.

selected dice
      ↓
new random values
      ↓
new groups

Предыдущие результаты сохраняются в истории операции.

⸻

12. Add

Количество добавляемых кубиков определяется выбором.

Selected = 5
→ Add 5

Если ничего не выбрано:

Add 1

Добавленные кубики получают случайное значение D6 и сразу попадают в соответствующую группу.

Во время свайпа на пустом столе `SwipeAddSession` хранит выданные значения. Если кубик удалён и затем снова добавлен в рамках того же жеста, он возвращается с прежним значением. Обычные действия Add и Delete не запоминают значения удалённых кубиков.

Последовательные нажатия Add и Delete в рамках одного раунда сворачиваются в одну net-запись. Например, «Add 5 → Delete 2 → Add 1» — одна запись «Add 4».

⸻

13. Delete

Если выбраны кубики:

Delete X

удаляются выбранные кубики.

Если выбор отсутствует, кнопка может использоваться для удаления последнего добавленного пользователем кубика.

⸻

14. Move

Move заменяет Boost.

Move позволяет переместить выбранные кубики в любую группу.

Selected dice
      ↓
Move
      ↓
Target group

Перетаскивание начинается после удержания кубика в течение input.dragDelayMs (по умолчанию 1000 мс). При перетаскивании (drag) захваченный кубик увеличивается и следует за курсором; если перемещается несколько кубиков, они собираются в компактную группу у курсора и следуют за ним. Плашка группы, над которой находится перетаскиваемый кубик, подсвечивается (selectedOpacity). Прозрачность плашек групп меняется плавно (config.renderer.plate.fadeMs). Если кубики отпущены не над новой группой, они возвращаются на исходные места, а выделение снимается. Это визуальное состояние Renderer и не влияет на игровое состояние.

Это изменение является логическим действием и должно отображаться в истории.

⸻

15. Enhance

Enhance увеличивает значение выбранных кубиков на единицу.

Enhance — частный случай Move (перемещение на +1). В текущем MVP не реализуется.

Поведение при достижении максимального значения должно быть определено отдельно в игровых правилах и не должно быть зашито в Renderer.

⸻

16. Undo / Redo

Undo и Redo работают независимо от пользовательской истории.

Undo/Redo не отображаются в History.

Undo:

Current State
     ↓
Previous State

Redo:

Previous State
     ↓
Next State

После нового действия после Undo ветка Redo удаляется.

Глубина Undo/Redo не ограничивается архитектурой.

Undo/Redo осуществляют переход по истории действий: отменяемы все действия, кроме выбора.

Последовательные одинаковые действия объединяются в один отменяемый шаг (например, пять нажатий «Add 1» — одно действие; свайп на пустом столе — одно действие).

⸻

17. History

History записывает каждое действие игрока: Roll, ReRoll, Add, Delete, Move и Clear.

Undo/Redo переходят по этой же истории и не записываются как отдельные записи.

Add и Delete в рамках одного раунда сворачиваются в одну net-запись: «Add 5 → Delete 2 → Add 1» записывается как «Add 4».

Пример строки броска:

20d6 → Roll → 4d6:6 → ReRoll → 2d6:6 → Explode

4d6:6 означает:

4 кубика со значением 6

⸻

18. Info Panel

Верхняя область интерфейса — короткий «живой фид» последних действий игрока (до трёх строк).

Нижняя строка всегда актуальна: последнее действие → общее число кубиков → что выбрано.

При новом игровом действии (Roll, ReRoll, Add, Delete, Move, Clear) нижняя строка замораживается и сдвигается на строку выше; снизу формируется новая. Строки идут хронологически: старые выше, актуальное на нижней строке.

Select не создаёт строку: он только обновляет сегмент «что выбрано» на нижней строке.

Верхние строки — только действие (без повтора «всего» и «выбор»).

Roll и Clear — граница итерации: фид показывает действия текущей итерации (вычисляется по последней записи roll/clear в history).

Фид пересчитывается автоматически при Undo/Redo, так как history является частью Game State.

Формат строк — английский, варгейм-нотация:

Roll {count} {что затронуто} → {результат}; Reroll {count} {что затронуто} → {результат}; Add {count} [→ {результат}]; Remove {count} [{что удалено}]; Move {count} [{что затронуто}] → {value}; Clear.

Roll и ReRoll хранят в записи истории значения брошенных кубиков: HistoryEntry.after (roll, reroll, add) и HistoryEntry.before (roll, reroll, move, delete) — значения затронутых кубиков до действия. «Что затронуто» и «результат» форматируются той же нотацией, что и показ выделения: результат — группировка по убыванию `6×2, 5, 3`; «что затронуто» — полностью взятая группа `6+`, частично взятая `some of 6` / `some of 4-6`. Частичность определяется по HistoryEntry.totals — сколько кубиков каждого затронутого значения было на столе в момент действия.

Последний шаг текущей итерации в развёрнутой истории не повторяется: его показывает живая нижняя строка и сами кубики на столе.

Пример нижней строки:

Reroll 4 6+ → 6×2, 5, 3 · 25 dice · Sel 4 (6+)

Сегмент «что выбрано»: 6, 5 и 4 → `4+`; только 6 → `6+`; одиночное не-6 → `5s`; смежный диапазон без 6 → `1-3`; несмежные — перечисление `6, 4`. Пустой выбор сегмент не показывает.

Длинные строки (результат Roll/Reroll) сжимают шрифт до минимума и обрезаются.

Пустой стол — подсказка вместо строк.

Информационная область не обязана постоянно показывать полную History.

Она является информационной областью и в будущем может отображать различные режимы:

History
Statistics
Current results
Quotes
Messages

Пример декоративного контента:

"Лучше умереть за Императора,
чем жить для себя."

История является опциональной частью UX.

⸻

19. Operation Colors

Операции после первого броска должны визуально различаться.

Например:

Roll
ReRoll
Move
Explode

могут получать разные оттенки.

Кубики, созданные или изменённые соответствующей операцией, получают тот же визуальный оттенок.

Цветовая система должна быть централизованной и не зашиваться непосредственно в игровые операции.

⸻

20. Game Table

Игровой стол является представлением Game State.

Для D6:

6 ─────────────────
5 ─────────────────
4 ─────────────────
3 ─────────────────
2 ─────────────────
1 ─────────────────

Кубики автоматически располагаются в соответствующей группе.

Количество кубиков в группе является производным от Game State.

⸻

21. Input

Input Layer преобразует действия пользователя в команды приложения.

Он должен быть отделён от Core.

Поддерживаемые взаимодействия:

Tap
Long Press
Drag
Swipe
Group Tap
Group Swipe

Конкретные жесты могут изменяться без изменения Core.

Различение жестов на кубике:

- тап (отпустил до движения за dragThresholdPx и до истечения dragDelayMs) — выбор;
- свайп (движение за dragThresholdPx до истечения dragDelayMs) — выбор диапазона от начального до текущего кубика, обновляется в реальном времени по мере движения пальца; после начала свайпа перетаскивание больше не активируется; при отпускании пальца не над кубиком выбранный диапазон сохраняется;
- удержание (прошло dragDelayMs до движения за порог) — начало перетаскивания.

Задержка перед перетаскиванием настраивается через input.dragDelayMs (по умолчанию 1000 мс).

Во время удержания в строке статуса показывается шкала, заполняющаяся от 0 до 100% за input.dragDelayMs — индикатор подготовки захвата. Как только начинается перетаскивание, шкала скрывается. На время жеста (удержание и перетаскивание) авто-сброс выделения по таймеру приостанавливается; при отпускании пальца выделение снимается (перетаскивание) либо заменяется новым (тап/свайп).

Тап по уже выбранному кубику снимает выделение.

Свайп по группам (group swipe) выбирает диапазон групп от начальной до текущей, так же обновляясь в реальном времени.

⸻

22. Empty Table

Если поле пустое, отображается:

Swipe Finger to Add or Reduse Dices

Свайп работает только когда стол полностью пуст — после запуска или после Clear.

Пока палец находится на экране, количество кубиков изменяется в соответствии с движением.

Кубики сразу появляются в соответствующих группах.

Выданные значения запоминаются: кубик, удалённый и затем снова добавленный в рамках того же жеста, возвращается с прежним значением.

После отпускания пальца количество кубиков фиксируется, и весь жест считается одним действием.

⸻

23. UI

Основные элементы:

Info Panel
Game Table
Action Bar
Bottom Navigation

Основные действия:

[Roll]
[ReRoll]
[Add X]
[Move X]
[Delete X]
[Undo]
[Redo]
[Clear]

При отсутствии выбора:

[Roll] неактивен
[ReRoll] перебрасывает все кубики
[Add 1]
[Move 1]
[Delete]

При выборе:

[Add X]
[Move X]
[Delete X]

⸻

24. Renderer

Three.js отвечает только за визуальное представление.

Renderer получает состояние и отображает его.

Game State
    ↓
Renderer
    ↓
Three.js Scene

Renderer отвечает за:

* 3D-кубики;
* камеру;
* материалы;
* освещение;
* анимации;
* частицы;
* визуальное выделение;
* визуальный захват при перетаскивании;
* перемещение между группами;
* эффекты.

Renderer не принимает решения о результате броска.

⸻

25. Rendering Loop

DiceFlow не должен постоянно рендерить сцену.

Состояние:

IDLE

означает отсутствие постоянного render loop.

При необходимости:

IDLE
 ↓
ACTION
 ↓
requestAnimationFrame()
 ↓
Animation
 ↓
Finished
 ↓
IDLE

Rendering запускается только при:

* анимации;
* взаимодействии;
* перемещении;
* броске;
* эффектах;
* изменении визуального состояния.

Это необходимо для снижения энергопотребления.

WebGL-контекст не следует уничтожать при переходе в IDLE.

⸻

26. Pixel-art Rendering

MVP сначала разрабатывается без pixel-art post-processing.

Порядок:

3D Scene
 ↓
Materials
 ↓
Lighting
 ↓
Animation
 ↓
Game Feel
 ↓
Performance
 ↓
Pixel-art post-processing

Pixel-art является визуальным слоем Renderer и не должен влиять на Game Core.

⸻

27. Game Feel

Game Feel является отдельным слоем.

Он может включать:

* звуки;
* частицы;
* анимации;
* camera effects;
* визуальные реакции;
* разные реакции на результаты.

Game Feel не должен влиять на игровые правила.

Например:

Game Core:
Dice value = 6
Game Feel:
powerful sound
particles
animation

⸻

28. Storage

Приложение должно работать офлайн.

Локально могут храниться:

* текущая игра;
* история;
* версия данных;
* installation ID;
* кэш ресурсов.

Для структурированных данных предпочтительно использовать IndexedDB.

⸻

29. PWA

DiceFlow распространяется как PWA.

Требования:

* HTTPS;
* Web App Manifest;
* Service Worker;
* offline cache;
* installable application;
* cached application shell.

Основной сценарий:

First Launch
    ↓
Download
    ↓
Cache
    ↓
Install
Next Launch
    ↓
Local Cache
    ↓
Immediate Start

⸻

30. Startup

Приоритет:

1. показать интерфейс;
2. восстановить локальное состояние;
3. запустить приложение;
4. выполнить фоновые проверки;
5. загрузить необязательные ресурсы.

Аналитика, Remote Config и другие сетевые операции не должны блокировать запуск основного интерфейса.

⸻

31. Remote Config

Архитектура должна предусматривать удалённую конфигурацию.

Она может управлять:

Current Version
Minimum Version
Announcement
Image
Button
Feature Message
Maintenance Message

Пример:

Launch
  ↓
Show Application
  ↓
Fetch Remote Config
  ↓
Show optional message

Сообщение может содержать:

Image
Title
Text
Button

Новая версия приложения не требуется для изменения такого сообщения.

Remote Config не является частью игрового ядра.

⸻

32. Analytics

Архитектура должна позволять собирать:

Launches
Installation IDs
Sessions
Active Sessions
Platform
Application Version
Major Actions

Для определения активных пользователей используется heartbeat/session timeout.

Analytics не должна блокировать запуск приложения.

В MVP аналитика должна быть минимальной.

⸻

33. Future Store

Магазин не реализуется в MVP.

Архитектура должна позволять добавить:

Products
Content Packs
Purchases
Entitlements
Payment Provider

Store не должен быть частью Game Core.

⸻

34. Future Payments

Платёжная система подключается отдельным сервисным слоем.

UI
 ↓
Store Service
 ↓
Payment Provider
 ↓
Purchase Confirmation
 ↓
Entitlement
 ↓
Content Unlock

Игровое ядро не должно зависеть от конкретного платёжного провайдера.

⸻

35. Future Custom Dice

В будущем должна поддерживаться модель:

Dice Type
├── Faces
├── Values
├── Properties
├── Rules
├── Visual
└── Audio

MVP использует только:

D6

Но Core не должен содержать предположения, которые делают другие типы кубиков невозможными.

⸻

36. Future Content Packs

В будущем визуальные и аудио ресурсы могут быть объединены в Content Packs:

Content Pack
├── Dice
├── Background
├── Materials
├── Sounds
├── Particles
└── Effects

Content Packs не должны менять базовую игровую логику.

⸻

37. Future Feature Requests

Приложение может содержать:

Request Feature

Кнопка может вести:

* на GitHub;
* на страницу проекта;
* в Telegram;
* на другой выбранный канал связи.

GitHub интеграция не является частью MVP Core.

⸻

38. Future Notifications

Архитектура не должна исключать Web Push.

Push Notifications не реализуются в MVP.

Remote Config и сообщения при запуске должны работать независимо от Push.

⸻

39. Buy Me a Coffee

Кнопка поддержки разработчика является внешним действием.

Она не требует интеграции платежей в приложение и не относится к игровому Core.

⸻

40. Testing

Core должен тестироваться без браузера и без Three.js.

Минимальные тесты:

Roll
ReRoll
Add
Delete
Move
Enhance
Selection
Undo
Redo
Clear
History

Особенно важно тестировать переходы состояния.

State A
   ↓ Action
State B
   ↓ Undo
State A
   ↓ Redo
State B

⸻

41. Основной поток приложения

Launch
  ↓
Restore State
  ↓
Show Table
  ↓
User Input
  ↓
Action
  ↓
Core
  ↓
New Game State
  ↓
History
  ↓
UI Update
  ↓
Renderer Animation
  ↓
Idle

⸻

42. Главное архитектурное правило

DiceFlow должен состоять из трёх независимых концептуальных уровней:

┌───────────────────────────────┐
│             UI                │
│       React / Input           │
├───────────────────────────────┤
│          GAME CORE             │
│          TypeScript            │
├───────────────────────────────┤
│          RENDERER              │
│       Three.js / WebGL         │
└───────────────────────────────┘

Game Core определяет, что произошло.

Renderer определяет, как это выглядит.

UI определяет, как пользователь взаимодействует с приложением.

Ни один из этих уровней не должен брать на себя ответственность другого.

⸻

43. MVP Boundary

Реализуется

D6
3D Dice
Roll
ReRoll
Add
Delete
Move
Selection
Groups
Undo
Redo
Clear
History
Info Panel
Game Feel
Offline
PWA
Fast Startup
Low Battery Usage

Закладывается архитектурно, но НЕ реализуется

Custom Dice
Custom Face Properties
Content Packs
Store
Payments
Analytics Expansion
Remote Config Expansion
Push Notifications
GitHub Integration
Accounts
Enhance (частный случай Move)
Explode (частный случай Add)

Не требуется

Physics-based Dice
Backend для игровой логики
Realtime Multiplayer
Постоянный Render Loop

Игровое ядро должно оставаться полностью локальным.

⸻

44. Критерий качества

DiceFlow должен ощущаться как:

Открыть
  ↓
Мгновенно готово
  ↓
Выбрать кубики
  ↓
Roll
  ↓
Мгновенно увидеть результат
  ↓
Изменить
  ↓
Следующий Roll

Технологии существуют для этого опыта, а не наоборот.
